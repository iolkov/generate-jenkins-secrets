# generate-jenkins-secrets

## Usage
```
go run main.go
...
master.key: base64(xxx)
secret.key: base64(xxx)
hudson.util.Secret: base64(xxx)
com.cloudbees.plugins.credentials.SecretBytes.KEY: base64(xxx)
```
